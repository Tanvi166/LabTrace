# Agents

LabTrace keeps orchestration in application code: Coordinator sequences metadata, code, results, reproducibility, and report agents. Foundry, when configured, is a model provider, not a duplicate agent system. Deterministic evidence remains authoritative; external RAG evidence must retain source metadata.
