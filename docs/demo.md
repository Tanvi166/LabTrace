# Demo

Create an experiment, upload safe artifacts, run deterministic analysis, run AI analysis, then ingest a knowledge document as an administrator and search it from RAG Knowledge. Local results are grounded in stored document chunks and display their source metadata.
