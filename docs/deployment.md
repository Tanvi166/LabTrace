# Deployment and demo

Local mode uses SQLite, local storage, mock model behavior, and local knowledge retrieval. Azure mode requires explicitly configured resources and SDK packages; it is not enabled merely by setting a UI label. Run backend tests with `python -m pytest tests/ -v` and build the frontend with `npm run build`.
