# MCP

The pre-existing MCP API is a placeholder and is not a conformant MCP server yet. Before external MCP exposure, tools must authenticate callers, enforce experiment ownership, use the deterministic service layer, and never execute uploaded code.
